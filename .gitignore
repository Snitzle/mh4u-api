*.log
.DS_Store
.env
.env.backup
.env.production
.phpactor.json
.phpunit.result.cache
/.codex
/.cursor/
/.idea
/.nova
/.phpunit.cache
/.vscode
/.zed
/auth.json
/node_modules
/public/build
/public/fonts-manifest.dev.json
/public/hot
/public/storage
/storage/*.key
/storage/pail
/vendor
_ide_helper.php
Homestead.json
Homestead.yaml
Thumbs.db

# Generated by `php artisan mh4u:sync-assets` from the vendored source tree
/public/assets

# Scribe generated docs (regenerated via `php artisan scribe:generate`; the
# editable source lives in .scribe/)
/public/vendor/scribe
/resources/views/scribe

# Local data sources — never committed (DMCA hygiene). The scraped Kiranico JSON
# (populated from the mh4u-kiranico-scraper output) and the full MySQL dump
# produced by `php artisan mh4u:export-sql`. The bundled mh4u.db is still tracked
# for now; it will be untracked once the dump becomes the source of record.
/database/source/kiranico/
/database/source/mh4u_full.sql
